variables:
  CI_REGISTRY_IMAGE: registry.phpcreation.com/mysql-s3-backup

services:
  - docker:dind

stages:
  - build

build:
  only:
    - master
  stage: build
  script:
    - docker build -t $CI_REGISTRY_IMAGE:latest .
    - docker push $CI_REGISTRY_IMAGE:latest
